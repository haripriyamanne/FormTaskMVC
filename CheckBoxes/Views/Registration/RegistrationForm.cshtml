@model CheckBoxes.Models.RegistrationModel

@{
    ViewData["Title"] = "RegistrationForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 style="text-align: center">Web Design Questionnaire</h1>

<div class="row">

    <form asp-action="RegistrationForm">
        <div class="form-group">
            <label asp-for="ServiceAreas" class="control-label"></label><br />

            @*@foreach (var item in Model.ServiceAreas)
            {
               <input id="@(item.Value)"
                      type="checkbox"
                      value="@item.Value"
                      checked="@item.IsChecked" />
               @item.Text <br />
            }*@
            @for (int i = 0; i < Model.ServiceAreas.Count; i++)
            {
                @Html.CheckBoxFor(m => m.ServiceAreas[i].IsChecked)
                @Model.ServiceAreas[i].Text
                @*@Html.DisplayFor(m => m.ServiceAreas[i].Text)*@
                @Html.HiddenFor(m => m.ServiceAreas[i].Value)
                @Html.HiddenFor(m => m.ServiceAreas[i].Text)
                @*<label class="control-label ">@Model.ServiceAreas[i].Text </label>*@
                <br />
            }
            <span asp-validation-for="ServiceAreas" class="text-danger"></span>
        </div>
       
        <div class="form-group">
            <input type="submit" value="SEND" class="btn btn-dark" />
        </div>
    </form>
</div>

           